provider "aws" {
    access_key = "${var.aws_access_key}"
    secret_key = "${var.aws_secret_key}"
    region = "${var.region}"
}

module "s3" {
    source = "/"
}


leg extension
Angled Leg Press
Hamstring
Dumble Lunges

Incline Reverse Crunch
ELbow to knee twist
leg lift
Bicycle kick